# Broken MDX 5MB Examples

This pack contains intentionally malformed `.mdx` files totaling around 5MB uncompressed.

Use it to test that EXT:
- Opens broken MDX as plain editable text.
- Does not crash preview, outline, indexing, or search.
- Preserves the original broken source text unless the user edits it.
- Keeps large-file safeguards active.
