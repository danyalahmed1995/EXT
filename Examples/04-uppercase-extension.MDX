# Uppercase Extension Test

This file intentionally uses `.MDX`.

export const extensionCase = 'uppercase'

<Demo enabled />

## Expected

EXT should treat this the same as `.mdx` if extension handling is case-insensitive.
